{
  "name": "aicc-topic-recommender",
  "private": true,
  "workspaces": ["client", "client/server"],
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w aicc-server\" \"npm run dev -w aicc-client\"",
    "build": "npm run build -w aicc-client && npm run build -w aicc-server",
    "start": "npm run start -w aicc-server",
    "vercel-build": "npm run build -w aicc-client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=18"
  }
}
