# 이 파일을 복사:  copy .env.example .env  (PowerShell: Copy-Item .env.example .env)
# 그 다음 OPENAI_API_KEY 에 본인 키를 넣으세요.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
PORT=3001

# 비우면 로컬에서 모든 Origin 허용
ALLOWED_ORIGINS=

MONGODB_URI=
MONGODB_DB=aicc
MONGODB_COLLECTION=generated_sessions
